import React from 'react';
import ProductPageLayout from '../components/product/ProductPageLayout';
import { per } from '../data/products/per';

const PERPage: React.FC = () => <ProductPageLayout data={per} />;

export default PERPage;
